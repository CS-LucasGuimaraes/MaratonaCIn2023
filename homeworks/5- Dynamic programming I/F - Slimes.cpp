#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    int n; cin >> n;

    int a;

    vector<int> slimes;

    for (int i = 0; i < n; i++) {
        cin >> a;

        slimes.push_back(a);
    }



    return 0;
}